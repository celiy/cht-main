---
description: Revisar mudanças git por breaking changes nos consumidores
---

Siga a skill do workspace em `.cursor/skills/code-review/SKILL.md`.

Leia o arquivo primeiro. Colete as mudanças de **todos** os repositórios git do workspace
(lembre que `cht-*` são repos separados e o `.gitignore` da raiz os exclui), não só do
`cht-main`.

O único objetivo é detectar breaking change para consumidores do monorepo.
