---
description: Ciclo TDD (RED, GREEN, REFACTOR) com evidência
---

Siga a skill do workspace em `.cursor/skills/tdd-workflow/SKILL.md`.

Leia o arquivo primeiro. Se o usuário passou um caminho de plano ou descreveu o comportamento
alvo, use isso como ponto de partida; senão, pergunte o que deve ser implementado.

Respeite a skill: se a mudança for de front, onde não há runner de teste configurado, diga
isso em vez de inventar um teste que não roda.

Não escreva no git — apenas proponha os commits ao final.
